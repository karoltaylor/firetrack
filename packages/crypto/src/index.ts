/**
 * Encrypts plaintext using AES-256-GCM.
 * Placeholder — full implementation in Epic 8 (Story 8.1).
 */
export async function encrypt(_plaintext: string, _key: string): Promise<string> {
  return '';
}

/**
 * Decrypts ciphertext using AES-256-GCM.
 * Placeholder — full implementation in Epic 8 (Story 8.1).
 */
export async function decrypt(_ciphertext: string, _key: string): Promise<string> {
  return '';
}
