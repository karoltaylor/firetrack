/**
 * Generates a fingerprint from CSV/Excel column headers to remember column mappings.
 * Placeholder — full implementation in Epic 3 (Story 3.3).
 */
export function generateHeaderFingerprint(_headers: string[]): string {
  return '';
}
